{
  "name": "action-check-domain",
  "scripts": {
    "build": "ncc build index.ts --minify"
  },
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.2"
  }
}
